import { Box } from "@mui/material"

export default function PlaidConfirm (props) {

  /* When this page loads: 
    - Attempt to log in with the current application
    - If an e-mail already exists, we should get an error. Inform the user to log in to their portal continue or create a new application
    
  
  */
  return (
    <Box>
      <p>Plaid confirm</p>
    </Box>
  )
}
